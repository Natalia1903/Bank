package pages.bankManagerLoginPage.customers;

import org.openqa.selenium.WebDriver;
import pages.PageBase;

public class CustomersPage extends PageBase {
    public CustomersPage(WebDriver driver) {
        super(driver);
    }
}
